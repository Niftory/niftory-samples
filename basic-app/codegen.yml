overwrite: true
schema:
  - ${NEXT_PUBLIC_API_PATH}:
      headers:
        X-Niftory-API-Key: ${NEXT_PUBLIC_API_KEY}

documents: "./**/*.tsx"
generates:
  generated/graphql.ts:
    plugins:
      - add:
          content:
            - "// @ts-nocheck"
      - typescript
      - typescript-operations
      - typescript-react-query
    config:
      fetcher: graphql-request
